name: Java test

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: |
          find src/test/java -type f -name "*.java" > tests.txt
          javac -d out @tests.txt